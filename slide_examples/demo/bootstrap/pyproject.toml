[project]
name = "mattsbackend"
version = "0.1.0"
description = "A custom build backend example"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "build"
backend-path = ["mattsbackend"]